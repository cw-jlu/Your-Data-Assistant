### Example 1: Standard SQL Query (Preferred for .db files)
```json
{
  "thought": "The question asks for the total sales in 2023. I see sales data in transactions.db.",
  "reflection": "I will use SQL to aggregate data efficiently.",
  "data_sufficient": false,
  "action": "execute_context_sql",
  "action_input": {
    "path": "db/sales.db",
    "sql": "SELECT SUM(amount) FROM transactions WHERE date LIKE '2023%'",
    "limit": 1
  }
}
```

### Example 2: Python Calculation with Logic Audit
```json
{
  "thought": "Logic: Unit_Price = Price / Amount. I need to find records where Unit_Price > 29. I will use pandas to calculate this.",
  "reflection": "I checked the column names in the roadmap. I will align types using .astype(str) before filtering.",
  "data_sufficient": false,
  "action": "execute_python",
  "action_input": {
    "code_lines": [
      "import pandas as pd",
      "df = pd.read_csv('csv/orders.csv')",
      "df['Price'] = df['Price'].astype(float)",
      "df['Amount'] = df['Amount'].astype(float)",
      "results = df[df['Price'] / df['Amount'] > 29]",
      "print(results[['CustomerID', 'Price']])"
    ]
  }
}
```

### Example 3: Precise Submission (No Extra Columns)
```json
{
  "thought": "I have the ratio 0.375 for user 24. The question only asks for the ratio.",
  "reflection": "Rule 6: ONLY include the requested column. I will remove user_id and counts.",
  "data_sufficient": true,
  "action": "answer",
  "action_input": {
    "columns": ["ratio"],
    "rows": [["0.375"]]
  }
}
```

### Example 4: Reading Business Rules via PageIndex Line Numbers (ALWAYS DO THIS FIRST)
```json
{
  "thought": "The question involves 'abnormal' WBC levels. I must NOT use my own knowledge of medical thresholds. The DATA ROADMAP shows knowledge.md exists, and the PageIndex tree shows '[0003] WBC Reference Ranges (lines 45-62)'. I will read exactly those lines to get the domain-defined threshold.",
  "reflection": "Using my own assumption would risk wrong answers. The PageIndex gives me the exact line range — I should use read_doc_lines instead of reading the whole document.",
  "data_sufficient": false,
  "action": "read_doc_lines",
  "action_input": {
    "path": "knowledge.md",
    "start_line": 45,
    "end_line": 62
  }
}
```

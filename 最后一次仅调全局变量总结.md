# KDD Cup 2026 Data Agent 错误任务深度解析报告 (100% 全量版)

本报告汇总了对 `20260427T131119Z` 批次中 **17 个** 失败任务的逐一审计结果。

---

## 1. 执行与交互层故障 (Total: 6)

| 任务 ID | 现象 | 核心病因 (Root Cause) |
| :--- | :--- | :--- |
| **task_24 / 27** | 超时 | **JSON 语法死循环**：Qwen 输出长代码导致 JSON 格式损坏，反复修正占满 Token，最终 1200s 超时。 |
| **task_163 / 259** | 熔断 | **5 次解析错误**：在代码转义上彻底卡死，触发熔断保护。 |
| **task_396** | 步数耗尽 | **正则表达式地狱**：在非结构化叙事 MD 中陷入无限正则调试，35 步走完未得出答案。 |
| **task_418** | 统计错误 | **数据关联缺失 + 格式错位**：计算年龄时漏掉了部分出生日期文件，且提交格式（单数 vs 细分）与要求不符。 |

---

## 2. 逻辑、语义与行为缺陷 (Total: 11)

| 任务 ID | 现象 | 核心病因 (Root Cause) |
| :--- | :--- | :--- |
| **task_173** | 无结果 | **大文件“懒政”**：看到 8MB CSV 且没有显式数据库关联，主观谎称“无数据”。 |
| **task_180** | 召回低 | **截断偷懒**：算出 153 行，只提交了前 20 行，导致得分 0。 |
| **task_86** | 答案错 | **选表错误**：查 `driverStandings`（积分榜）而非 `results`（参赛结果）。 |
| **task_200** | 数量错 | **硬编码假设**：未经查询直接假设三键符号为 `#`，导致漏掉所有真实数据。 |
| **task_169** | 数值错 | **指标误读**：分不清“细分市场总消费”与“每个客户平均消费”。 |
| **task_80** | 车号错 | **盲目匹配**：在无 100% 匹配时，按数学差值强行关联，未反思逻辑。 |
| **task_25** | 计算错 | **语义盲区**：无法理解跨 JSON 与 CSV 的隐式字段映射关系。 |
| **task_89** | 时间错 | **单位/含义误用**：提交了“领先冠军的时间间隔 (+14s)”而非“完赛总时间”。 |
| **task_199** | 学校错 | **过滤层级歧义**：将“学校平均分”过滤应用到了“学区平均分”上。 |
| **task_344** | 数量错 | **常识凌驾文档**：硬编码了 WBC 的正常范围，无视了 `knowledge.md` 中的领域规定。 |
| **task_379** | 格式错 | **手动转录失误**：在 Python 算出结果后，手动填写 JSON 答案时写错了 ID 和行数。 |

---

## 3. 下一步行动纲领

1.  **[强制] SchemaNavigator**：扫描所有字段取值（DISTINCT）和跨表语义，生成关联地图，消除“盲区”和“假设”。
2.  **[强制] Anti-Laziness Prompt**：明确禁止截断提交和对大文件的虚报。
3.  **[工具] 答案自动化**：禁止 Agent 手写 JSON rows，必须由 Python 脚本生成变量直接传入。
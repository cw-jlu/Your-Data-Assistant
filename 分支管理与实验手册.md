# KDD Cup 2026：控制变量实验分支管理手册

为了系统性地优化 Agent，我们采用"以 main 为原点，一分支一变量"的策略进行独立实验。

---

## 1. 分支全景图 (Branch Overview)

```
                    ┌── feat/db-schema-only  (Schema 知识图谱)
                    │
                    ├── exp/pagerag          (PageRAG 文档检索)
main ──────────────┤
                    ├── exp/graphrag         (GraphRAG 三元组抽取)
                    │
                    └── exp/schema-std       (Schema 标准化预处理)
```

| 分支 | Commit | 实验变量 | 核心模块 | 目标死穴 |
| :--- | :--- | :--- | :--- | :--- |
| `feat/db-schema-only` | `084c554` | Schema 知识图谱 (JOIN Hints + knowledge.md) | `db_navigator.py` | task_86, task_173 |
| `exp/pagerag` | `d155a95` | 长文档分块检索 | `pagerag.py` | task_396, task_214 |
| `exp/graphrag` | `0c4424a` | LLM 三元组抽取 + Schema 融合 | `graphrag.py` | 跨模态关联任务 |
| `exp/schema-std` | `f68e5ef` | 编码标准化 + 列名映射 | `schema_std.py` | task_214 (编码), task_418 (格式) |

---

## 2. 切换测试流程

### 2.1 切换到指定实验分支
```bash
git checkout feat/db-schema-only   # Schema 知识图谱
git checkout exp/pagerag            # PageRAG
git checkout exp/graphrag           # GraphRAG
git checkout exp/schema-std         # Schema 标准化
```

### 2.2 运行验证
```bash
uv run python run_failed.py
```

### 2.3 查看当前分支
```bash
git branch -v
```

---

## 3. 回到分叉点 (main)

### 3.1 直接切回
```bash
git checkout main
```

### 3.2 查看某分支与 main 的分叉点
```bash
git merge-base main feat/db-schema-only
```

### 3.3 如果当前分支改乱了，丢弃所有未提交修改
```bash
git checkout -- .
git clean -fd
```

---

## 4. 排列组合实验（后续）

当单变量实验完成后，可以通过 cherry-pick 进行组合：
```bash
# 例：在 Schema KG 基础上叠加 PageRAG
git checkout feat/db-schema-only
git cherry-pick exp/pagerag
```

或者从 main 创建新的组合分支：
```bash
git checkout main
git checkout -b exp/schema-kg+pagerag
git merge --no-ff feat/db-schema-only
git merge --no-ff exp/pagerag
```

---

## 5. 实验注意事项

1. **隔离输出**：每次测试前建议清理 `artifacts/runs/` 或使用不同 run_id。
2. **先提交再切换**：切换分支前务必 `git add -A && git commit`，否则未提交的修改会带到新分支。
3. **不要在实验分支上改 main 的代码**：保持控制变量的纯洁性。

# KDD Cup 2026：数据 Agent 实验设计与技术白皮书

本手册旨在指导开发者通过“控制变量法”系统性地优化 Agent。不仅包含分支操作，更包含核心算法的深度解析。

---

## 一、 实验核心技术栈 (Core Technologies)

### 1. Schema 知识图谱 (Knowledge Graph)
**分支**：`feat/db-schema-only`
- **实现细节**：
    - **同名键关联 (JOIN Hints)**：遍历所有数据源（DB/CSV/JSON），将字段名作为 Key。若某个字段在超过一个数据源中出现，则在提示词中生成：`'driver_id' links: driver_standings.db <--> results.csv`。
    - **业务规则对齐**：针对 `knowledge.md`，使用正则提取包含 `:` 或 `-` 的定义行（如 `DNF: Did Not Finish`），防止 Agent 对业务缩写产生幻觉。
- **Prompt 样例**：
  ```text
  [JOIN HINTS] Shared columns across sources:
    'raceId' links: races.csv <--> results.db
  ```

### 2. PageRAG (RRF + LLM Rerank v2.2)
**分支**：`exp/kg+pagerag`
- **实现细节**：
    - **并行摘要索引 (Parallel Indexing)**：启动时通过 `ThreadPoolExecutor` 并行生成所有文档的摘要，显著降低初始化延迟（由 O(N) 降至 O(1) 左右）。
    - **RRF 融合检索 (Reciprocal Rank Fusion)**：融合 Vector (标题) 与 BM25 (正文) 的排名，平衡语义关联与关键词匹配。
    - **LLM Rerank**：对 RRF 召回的 Top-K 片段进行二次精准排序，剔除无关干扰信息。
    - **触发机制**：仅对 `context/doc/` 目录下的 `.md` 文件生效。
- **解决痛点**：精准处理多语种说明文档，彻底解决大规模文档集的初始化超时问题。

### 3. GraphRAG (LLM 驱动的三元组抽取)
**分支**：`exp/graphrag`
- **实现细节**：
    - **异步预提取**：在任务开始前，调用 LLM 对非结构化文档进行一次扫描，提取 `(主体)-(关系)-(客体)`。
    - **三元组格式**：强制要求模型输出 `[["Molecule", "hasID", "123"], ...]` 这种严格格式，并将其转化为自然语言描述注入。
- **解决痛点**：识别隐性的业务关联，例如从一段描述中推断出“某个分值”对应“某个数据库状态码”。

### 4. Schema 标准化 (Pre-processing)
**分支**：`exp/schema-std`
- **实现细节**：
    - **强制编码转换**：在 Agent 启动前，将所有非 UTF-8 文件（尤其是 Windows 常见的 GBK）转换为 UTF-8，并备份原文件。
    - **列名标准化 (Snake Case)**：将 `FirstName`, `Last-Name` 统统建议映射为 `first_name`, `last_name`。虽然不改原文件，但在 Roadmap 中提示 Agent 这种转换模式，提升其编写 Python 代码的成功率。

---

## 二、 组合实验逻辑 (Hybrid Strategies)

### 模式 E：KG + PageRAG (推荐方案)
- **运行逻辑**：
    1. 使用 KG 扫描所有表的 Schema 和 `knowledge.md`。
    2. 对剩余的长文档（如说明书、背景资料）使用 PageRAG 进行按需召回。
- **适用场景**：既有复杂的数据库 Join，又有超长背景文档的任务。

---

## 三、 分支切换与排列组合指南

### 1. 基础切换
```bash
git checkout <分支名>
uv run python run_failed.py
```

### 2. 手动创建排列组合 (Permutations)
如果你想把分支 A 的功能 and 分支 B 的功能合并：
1. **新建分支**：`git checkout -b exp/hybrid-a-b`
2. **合并代码**：`git merge feat/db-schema-only`
3. **继续合并**：`git merge exp/pagerag`
4. **冲突解决**：在 `react.py` 中，通常只需将两个导航器的输出字符串拼接即可：
   ```python
   data_roadmap = kg_roadmap + "\n" + pagerag_output
   ```

## 待办实验与调优 (Next Steps)
1. **模块化参数调优**：
   - 针对 `exp/kg+pagerag` 分支，对比 `rag_top_k` 为 3, 5, 7 时的 Score 变化。
   - 调整 `db_navigator` 中的样本数据量，观察对 Context Window 的影响。
2. **效果对比实验**：
   - 依次在所有分支运行 `run_failed.py`，记录成功率 and 平均得分，更新实验记录表。
3. **GraphRAG 置信度阈值**：
   - 测试将 `min_confidence` 从 0.5 提升至 0.7 对减少无效推理的帮助。

---

## 四、 故障排除 (Troubleshooting)

- **GBK 编码报错**：已在 `run_failed.py` 入口处强制 `sys.stdout` 为 UTF-8。若仍报错，请检查终端模拟器的编码设置。
- **Context 溢出**：若组合了多个 RAG 分支，Roadmap 可能会过长。此时需减小 PageRAG 的 `top_k` 参数或限制三元组的提取数量。
- **JSON 解析失败**：若 Agent 观察到 Roadmap 后输出变得过于复杂，请在 `react_system_prompt.txt` 中强化 JSON 格式要求。本项目已在 `react.py` 中引入了 **Greedy Brace Fix (贪心括号修复)** 和字面换行符转义逻辑，能够从模型截断的响应中自动恢复。
- **大文件读取超时**：对于超过 1MB 的数据集，本项目已在 `db_navigator` 中实现了 **Partial Peeking (部分探测)**，仅读取 JSON 前 10KB 提取列名，彻底避免扫描阶段的内存 OOM。在执行阶段，Agent 应优先使用 `pandas` 进行向量化处理。

---

## 五、 测试与评估指南 (Testing & Evaluation)

### 1. 运行实验

切换到对应分支后，使用以下命令运行失败任务集（冒烟测试）或全量测试：

```powershell
# 运行先前失败的任务（快速验证）
uv run python run_failed.py

# 运行全量 Benchmark
uv run dabench run-benchmark --config configs/react_baseline.local.yaml
```

### 2. 自动化评分与对比

运行结束后，使用 `evaluate.py` 获取符合官方 Leaderboard 规则的评分报告：

```powershell
# 评估最新一次运行结果
uv run python evaluate.py --save-md
```

**指标解读**：
- **Score (平均得分)**：综合 Recall 和 Penalty 的最终得分（官方排名依据）。
- **Recall (召回率)**：预测列与标准答案列的匹配程度。
- **Penalty (惩罚项)**：对预测中“多余列”的惩罚（λ=0.5）。
- **成功率 vs 准确率**：前者看是否跑完，后者看是否 Score=1.0。

### 3. 结果定位

每个分支的详细日志位于 `artifacts/runs/<run_id>/report.md`，您可以直接对比不同导航策略在 `Missing Columns` 和 `Mismatched Columns` 上的表现差异。

---

## 六、 注意事项 (Important Notes)

1. **环境同步**：每次切换分支后，建议执行一次 `uv sync` 确保依赖一致。
2. **调参建议**：可通过修改 YAML 中的 `agent.rag_top_k` 全局调整 RAG 召回深度。

---

## 七、 实验记录 (Benchmarks)

在第一阶段（A-board）评测中，我们对不同分支进行了提交测试，得分结果如下：

| 版本 (分支) | 对应提交版本 | 公开测试集表现 (本地) | A-board (隐藏集) 得分 | 方案特点与结论 |
| :--- | :--- | :--- | :--- | :--- |
| **v3** | `team1121_v3` | 53.50% (S: 56.91%) | **0.5114** ⭐ (最高) | **Schema KG + PageIndex (v3 分支)**。最稳定、效果最好的方案。融合了数据库 Schema 知识图谱导航与文档索引树，包含 Answer 拦截校验，去掉了冗余的向量检索，在复杂推理和多表关联上最为准确。 |
| **v1** | `team1121_v1` | 72.38% | **0.3298** | **官方基础 Baseline (main 分支)**。仅进行轻量级微调，无高级 Schema 导航及文档切分索引，对复杂关联支持较弱。 |
| **v6** | `team1121_v6 (v5分支)` | 64.00% | **0.3184** | **v3 + Prompt 边界优化 (v5 分支)**。在 v3 基础上加入了对不等式/边界值的强规则约束。由于增加了 Prompt token 数且规则限制较死，模型灵活性有所下降，隐藏集得分不如 v3。 |
| **v4** | `team1121_v4` | — | **0.2982** | **v3 去掉分级策略 (v4 分支)**。与 v3 代码完全同源，但对所有难度任务统一注入完整的 Schema KG + PageIndex，导致低难度任务受到信息噪音干扰而发生幻觉或崩溃。 |
| **v5** | `team1121_v5 (LLMWIKI)` | 59.50% (S: 64.67%) | **0.1658** (最低) | **LLM Wiki 预处理模式 (LLMWIKI 分支)**。在公开集上表现极佳，但在隐藏集上由于预处理过度消耗 Token 和时间，导致大量任务超时未完成，且引入了多余的幻觉，表现最差。 |
| **exp/kg+pagerag** | - | 较低 | 未提交 | **向量检索融合方案**。引入了本地 Embedding 和 RAG。实验表明，本项目以结构化数据为主，非结构化文档少，引入 RAG 不仅使得 Docker 镜像体积暴增（从 260MB 升至 3GB），在精度和耗时上也无任何优势。 |
| 其他实验分支 | - | 较低 | 未提交 | 其他如 `exp/graphrag` 等三元组方案，由于 LLM 抽取耗时过长，容易在 A-board 2小时限时内超时，故均未提交。 |

### 核心结论：
1. **隐藏集与公开集表现背离**：LLMWIKI (v5) 在公开集成功率最高，但在 A-board 得分最低；而 v3 在公开集上得分一般，在 A-board 上却斩获最高分。设计方案时必须防止过拟合公开集。
2. **轻量与稳定性优先**：在断网且严格限时的评测环境下，任何冗余的预处理（如 Wiki 字典生成、向量库构建、三元组抽取）都会积攒时间延迟，最终因超时导致大量任务 0 分。
3. **向量化 (Embedding) 在此赛题为伪需求**：对于主要是数据库和 CSV 关联的任务，精确的 Schema KG 导航和基于行号定位的 PageIndex 远远优于基于语义相似度的模糊向量检索。


## 八、 打包与提交规范 (Packaging & Submission)

根据 KDD Cup 2026 官方《比赛规则与提交规范》（`rules.md`），本项目已配置好自动化的容器打包脚本，完全符合赛方对于 `linux/amd64` 平台和 `team_id_vN.tar.gz` 格式的要求。

### 1. 提交前优化（移除本地 Embedding）
> [!TIP]
> 🚨 **体积与速度优化核心技巧**：
> 如果当前分支**不需要使用本地的 Embedding 模型**（例如“现在没有 embedding”的逻辑），强烈建议从 `pyproject.toml` 的 `dependencies` 中剔除 `sentence-transformers` 依赖。
> - **效果**：这会阻止 Docker 构建时下载体积庞大的 PyTorch 和 NVIDIA CUDA 环境依赖。
> - **收益**：将构建下载量从 ~3GB 缩减到 ~100MB，最终生成的提交压缩包体积从几个 GB 骤降到 **~260MB** 左右，且打包速度从 20 分钟提升至 1 分钟以内！

### 2. 一键打包脚本
本项目提供了跨平台的一键打包脚本，会自动将当前代码和环境依赖构建为标准的赛方镜像并进行 `tar.gz` 压缩。

**对于 Windows 用户**：
```powershell
# 语法：.\package_submission.ps1 -TeamId <你的队伍ID> -Version <版本号数字>
.\package_submission.ps1 -TeamId team1121 -Version 2
```
*注：该脚本利用了 Python 内置的 `gzip` 模块进行无损压缩，完美避开了 Windows 跨平台压缩常见的编码或格式损坏问题。*

**对于 Linux / macOS 用户**：
```bash
# 语法：./package_submission.sh <你的队伍ID> <版本号数字>
./package_submission.sh team1121 2
```

### 3. 本地模拟测试 (Local Docker Testing)
在提交前，可以利用以下命令完全模拟赛方的评估环境（网络隔离、路径映射及环境变量注入）以验证镜像可用性：

```powershell
# 准备空目录
New-Item -ItemType Directory -Force -Path test_output
New-Item -ItemType Directory -Force -Path test_logs

# 运行验证 (以测试 task_11 为例，加入 -e BENCHMARK_LIMIT=1 限制任务数)
docker run --rm `
  -v "D:\code\python\kdd\public\input:/input:ro" `
  -v "D:\code\python\kdd\starter-kit\test_output:/output:rw" `
  -v "D:\code\python\kdd\starter-kit\test_logs:/logs:rw" `
  -e MODEL_API_URL="https://dashscope.aliyuncs.com/compatible-mode/v1" `
  -e MODEL_API_KEY="你的API_KEY" `
  -e MODEL_NAME="qwen-plus" `
  -e BENCHMARK_LIMIT=1 `
  team1121:v2
```

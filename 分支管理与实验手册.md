# KDD Cup 2026：实验分支管理与技术白皮书

本手册记录了所有优化分支的技术实现细节、设计动机及操作指南。

---

## 1. 核心技术模块说明 (Implementation Details)

### 📊 A. Schema 知识图谱 (Knowledge Graph)
**分支**：`feat/db-schema-only`
- **实现逻辑**：
    1. **结构扫描**：深度扫描 `.db`, `.csv`, `.json` 的表头与字段。
    2. **JOIN Hints**：自动发现不同文件间的同名字段（如 `driver_id`），在 Prompt 中显式提示 Agent 这是一个潜在的 JOIN 路径。
    3. **业务定义注入**：解析 `knowledge.md` 中的业务缩写与规则，建立“字段名 <-> 业务含义”的映射。
- **解决痛点**：Agent 选错表、找不到关联键、无法理解业务简写。

### 📖 B. PageRAG (分块检索)
**分支**：`exp/pagerag`
- **实现逻辑**：
    1. **分块 (Chunking)**：按 Markdown 的标题等级（# / ## / ###）将长文档切割。
    2. **关键词索引**：建立简易的关键词反向索引。
    3. **召回 (Retrieve)**：根据任务问题（Question）计算相关性得分，只返回最相关的 Top-3 分块。
- **解决痛点**：长文档（如 molecule.md）超出上下文窗口、Agent 在万字长文中迷失、正则调试地狱。

### 🕸️ C. GraphRAG (语义三元组)
**分支**：`exp/graphrag`
- **实现逻辑**：
    1. **LLM 抽取**：调用模型从非结构化文档中预先抽取 `(主体)-(关系)-(客体)` 三元组。
    2. **节点对齐**：将抽取的实体与数据库中的 Table/Column 节点进行语义对齐。
- **解决痛点**：处理跨模态关联（如：从一段描述中识别出它对应的数据库 ID）。

### 🧹 D. Schema 标准化 (Standardization)
**分支**：`exp/schema-std`
- **实现逻辑**：统一所有文件编码为 UTF-8，并将复杂的列名映射为标准 `snake_case`。

---

## 2. 组合实验分支 (Advanced Permutations)

### 🏆 E. KG + PageRAG (分工协作)
**分支**：`exp/kg+pagerag`
- **设计**：`knowledge.md` 由 KG 模块解析（因为它非常关键且通常不长）；其余 MD/TXT 文档由 PageRAG 处理。
- **优势**：既有了全局数据地图，又具备了处理超长说明文档的能力。

### 🧬 F. KG + GraphRAG (深度语义)
**分支**：`exp/kg+graphrag`
- **设计**：KG 提供硬核关联，GraphRAG 提供文档中的软语义关联。

---

## 3. 分支操作手册

| 分支名 | 切换命令 | 运行命令 |
| :--- | :--- | :--- |
| **主分支** | `git checkout main` | - |
| **知识图谱** | `git checkout feat/db-schema-only` | `uv run python run_failed.py` |
| **PageRAG** | `git checkout exp/pagerag` | `uv run python run_failed.py` |
| **GraphRAG** | `git checkout exp/graphrag` | `uv run python run_failed.py` |
| **KG + PageRAG** | `git checkout exp/kg+pagerag` | `uv run python run_failed.py` |
| **KG + GraphRAG** | `git checkout exp/kg+graphrag` | `uv run python run_failed.py` |

---

## 4. 如何查看详细实现代码
每个分支的核心差异点都位于：
- `src/data_agent_baseline/agents/` 目录下的专用模块（如 `pagerag.py`, `graphrag.py`）。
- `react.py` 中的集成入口。

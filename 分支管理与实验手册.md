# KDD Cup 2026：控制变量实验分支管理手册

为了系统性地优化 Agent，我们采用“一分支一变量”的策略进行分头测试。

---

## 1. 当前分支概览 (Active Branches)

| 分支名称 | 实验变量 | 目的 | 状态 |
| :--- | :--- | :--- | :--- |
| **`main`** | 无 (Baseline) | 作为对比的基准线。 | 稳定 |
| **`feat/db-schema-only`** | **Data Roadmap 注入** | 验证“全局数据结构可见性”对选表和探测逻辑的提升。 | **进行中** (已取得重大突破) |

---

## 2. 切换与测试流程 (Switching & Testing)

### 2.1 切换到特定实验
如果您想测试“全量数据地图”的效果：
```bash
git checkout feat/db-schema-only
```

### 2.2 运行验证脚本
切换分支后，建议使用专门的调试脚本运行之前失败的任务：
```bash
uv run python run_failed.py
```

### 2.3 查看当前实验代码
查看核心改动点：
- `src/data_agent_baseline/agents/db_navigator.py` (新增模块)
- `src/data_agent_baseline/agents/react.py` (集成逻辑)

---

## 3. 如何回到分叉点 (Resetting to Fork Point)

如果您想放弃当前的实验，或者想从同一个起点开启一个**新变量**的实验。

### 3.1 寻找分叉点 (Finding the base)
如果您不确定是从哪一笔 commit 开始分叉的，可以使用：
```bash
git merge-base main feat/db-schema-only
```
这会返回一个 commit ID，即为两个分支共同的祖先。

### 3.2 切回主分支
```bash
git checkout main
```

### 3.3 强行回滚 (非常规手段)
如果当前分支改乱了，想完全同步回 main 的状态：
```bash
git reset --hard origin/main
```

---

## 4. 实验注意事项 (Best Practices)

1.  **隔离 Artifacts**：运行不同分支的测试时，建议手动清理 `artifacts/runs/` 目录，或者在 `run_failed.py` 中指定不同的 `run_id`，防止数据混淆。
2.  **先文档后代码**：每次开启新分支，请先在 `CHANGES.md` 中记录该实验的假设。
3.  **合并策略**：只有当一个变量在 `run_failed.py` 中表现出明显的正向提升（如 80% 以上成功率）时，才考虑将其合并（Merge）回 `main`。

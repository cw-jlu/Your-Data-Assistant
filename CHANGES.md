# KDD Cup 2026 Data Agents - 技术改进与坑点总结

本项目已基于官方 Starter Kit 完成了针对评测环境的适配。以下是核心改动说明及开发过程中的经验总结。

---

## 1. 配置文件 (YAML) 参数说明
**⚠️ 结论：YAML 的参数结构（Schema）未发生任何变动。**

所有底层逻辑改进均在 Python 代码内完成，以保持与原始代码包的 100% 配置兼容。
- **参数名称与数量**：未增加、未减少、未重命名任何 YAML 配置项（即 `dataset`、`agent`、`run` 及其子项完全保留原始定义）。
- **兼容性**：您可以直接复用参赛包提供的 `react_baseline.example.yaml`。
- **配置建议**：本地开发时，建议在 YAML 中**注释掉** `run_id` 这一行，利用系统自动生成的带时间戳 ID 来避免目录冲突。

---

## 2. 核心逻辑改进对比 (Modification Comparison)

| 模块 | 原始逻辑 | 改进后逻辑 | 改进原因 |
| :--- | :--- | :--- | :--- |
| **Python 执行引擎 (Stability V2)** | 使用 `multiprocessing` 嵌套执行，并通过 `repr(code)` 注入字符串。 | 改用 **`subprocess` + 物理文件注入** 模式。 | **彻底消除死锁与转义错误**：解决嵌套进程导致的挂起问题，并避免了长代码中引号嵌套引发的语法解析崩溃。 |
| **分级执行策略 (Branching)** | 无论难度，统一加载全量 JSON-KG 和 PageRAG。 | **按难度分流**：Easy 任务仅加载文件列表；Medium/Hard 开启全量导航。 | **性能与准确度平衡**：减少简单任务中 2000+ Token 的噪音，防止简单题因“想太多”而超时或误判。 |
| **进程间通信 (IPC)** | 通过 `multiprocessing.Queue` 传输。 | 改用 **临时文件 (Temporary File)** 模式。 | **消除缓冲区死锁**：防止 Trace 数据过大堵塞系统管道。 |
| **配置化参数** | 硬编码在代码中的超时与阈值。 | 全部抽离至 `react_baseline.local.yaml`。 | **实验效率**：支持在不改代码的情况下快速调整 `python_timeout` 等关键参数。 |


---

## 3. 常见坑点汇总 (Pitfalls Summary)

| 坑点现象 | 根本原因 | 解决方案 |
| :--- | :--- | :--- |
| **任务 0.7s 即“成功”结束，但 `failure_reason` 为空** | **管道缓冲区死锁**。Agent 产生的推理 Trace 数据太大，导致子进程被堵塞在写管道这一步无法退出。 | 已修改 `runner.py`，改用全磁盘模式中转结果，不再受缓冲区容量限制。 |
| **`run-task` 命令直接退出，终端无任何输出信息** | **目录冲突**。YAML 中配置了固定 `run_id` 且对应目录已存在。`FileExistsError` 被 Cli 库静默拦截。 | 在 YAML 中注释掉固定 `run_id`，或手动删除旧的 artifacts 目录。 |
| **将 `max_workers` 改为 1 依然无法解决上述崩溃** | `max_workers` 控制的是外层多线程，而**死锁发生在任务内部的子进程通信层**。 | 必须修复 IPC 通信机制（方案 2），调节并发数无法触及根因。 |
| **`[SUBPROCESS] Task completed` 出现后系统永久卡死** | 死锁发生的具体位置：子进程完成核心推理正要向主进程返回数据包。 | 现在的逻辑已将“写回数据”与“进程 join”的操作完全解耦，彻底修复。 |

---

## 4. 后续开发建议
- **并发性能**：将 `max_workers` 设为 `4` 或 `8` 可充分利用 CPU 核心。新的临时文件方案是并发安全的。
- **本地调试**：可以直接运行 `uv run py main.py` 进行全量测试。
- **提交准备**：在导出镜像前，务必检查 `TODO.md`，将 `config.py` 中的模型配置切回环境变量优先模式，确保能读取到评测系统的 Key。

---

## 5. 模块化导航架构 V2 (Modular Navigation V2)

本项目在 V1 基础上进行了重构，解耦了导航逻辑，支持多种检索策略的“排列组合”。

| 模块 | V2 核心改进 | 解决的痛点 |
| :--- | :--- | :--- |
| **Schema KG** | 引入 `PRAGMA FK` 提取真实外键；增加行数统计与样本数据预览；解析 `knowledge.md` 业务定义。 | 解决 Agent 无法识别表关联和数据特征的问题。 |
| **PageRAG** | 升级为 **RRF + LLM Rerank v2.2**；支持并行化摘要提取。 | **解决初始化超时**：通过并行 LLM 调用将海量文档的摘要耗时降至常数级；通过 RRF 提升长文档检索精度。 |
| **JSON 稳定性** | 基础字符串转义逻辑。 | 新增 **Greedy Brace Fix** (贪心括号修复) 机制。 | **对抗截断**：解决模型在长输出时因被系统强行截断导致的 JSON 语法崩溃。 |
| **大文件扫描** | 完整加载 JSON 获取列名。 | 引入 **Partial Peeking** (正则部分探测)。 | **内存防爆**：解决 100MB+ JSON 文件扫描时导致的 OOM 或加载时间过长问题。 |
| **环境兼容** | `runner.py` 与 `python_exec.py` 强制开启 UTF-8 模式。 | 彻底解决 Windows 环境下处理多语言（韩、中等）字符的 `GBK` 崩溃。 |

| **PageIndex Lite** | 基于 Markdown 标题层级的树状索引；支持异步并发生成章节摘要。 | **超长文档导航**：解决了几百 KB Markdown 文件中“大海捞针”的难题，为 Agent 提供精准的物理行号坐标。 |

---

## 6. 路线图 JSON 化与历史压缩优化 (V3.0)

为了解决长任务场景下的 Token 膨胀和模型注意力涣散问题，本项目进行了深度工程优化。

| 改进项 | 核心逻辑 | 解决的问题 |
| :--- | :--- | :--- |
| **PageIndex Roadmap** | 针对 Extreme 难度，在 Roadmap 中注入文档树状结构。 | **盲目检索**：允许 Agent 像查书一样通过目录定位章节，而非盲目读取前 4000 字。 |
| **异步摘要 (Async)** | 使用 `asyncio` 并发生成章节语义摘要（Summary）。 | **初始化效率**：在极短时间内为 Agent 标注各章节的业务含义（如“此段包含 Creatinine 数据”）。 |
| **精准行读取工具** | 新增 `get_doc_structure` 和 `read_doc_lines` 工具。 | **定位精度**：Agent 可以根据 Roadmap 提供的行号，精准读取几十行核心数据，极大节省 Token 并减少噪音。 |
| **历史压缩 (Compression)** | 自动识别并折叠“已解决”的失败序列（即后面跟了成功步骤的），仅保留该序列的最后一次尝试。 | **上下文污染 vs 调试现场**：防止无效尝试堆积，同时确保 Agent 在连续报错时拥有完整的调试上下文。 |
| **PageRAG 退场** | 移除不稳定的 PageRAG 拦截逻辑，转向确定性的 PageIndex 导航。 | **稳定性**：消除了向量库在极端场景下的不确定性。 |
| **JSON 规范强化** | 在 Prompt 中增加 `Rule 21` 严禁字面量换行，并提供转义后的 Python 代码 Few-shot 示例。 | **解析崩溃**：显著降低了模型输出非法 JSON 导致的 Task Failed 率。 |


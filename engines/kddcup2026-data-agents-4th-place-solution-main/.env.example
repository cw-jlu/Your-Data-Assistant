# Paths (defaults work if running from project root)
DATASET_ROOT_PATH=data/public/input
RUN_OUTPUT_DIR=artifacts/runs

# Agent API credentials
AGENT_API_BASE=https://your-api-base-url/v1
AGENT_API_KEY=your-api-key

# Cloudflare Access headers (if required)
CF_ACCESS_CLIENT_ID=
CF_ACCESS_CLIENT_SECRET=

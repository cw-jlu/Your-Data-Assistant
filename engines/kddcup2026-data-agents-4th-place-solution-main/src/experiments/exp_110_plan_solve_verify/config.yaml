# Experiment parameters only.
# Environment-specific paths and credentials are in .env (see .env.example).

agent:
  model: qwen3.5-35b-a3b
  max_steps: 32
  min_steps: 4
  temperature: 0.0

run:
  # exp_110 PSV smoke / dev: single-attempt × workers=10 = 10 streams.
  # Switch back to (0.6, 0.6, 0.7) × workers=4 once pipeline is verified.
  max_workers: 10
  task_timeout_seconds: 900

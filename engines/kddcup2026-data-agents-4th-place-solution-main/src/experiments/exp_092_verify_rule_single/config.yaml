# Experiment parameters only.
# Environment-specific paths and credentials are in .env (see .env.example).

agent:
  model: qwen3.5-35b-a3b
  max_steps: 32
  min_steps: 4
  temperature: 0.0

run:
  # Single-attempt class for verify-rule axis. workers=10 (= 10 streams,
  # under vLLM ~13-15 saturation; matches PRIORITIES.md 57-59 new baseline).
  max_workers: 10
  task_timeout_seconds: 900

# Experiment parameters only.
# Environment-specific paths and credentials are in .env (see .env.example).

agent:
  model: qwen3.5-35b-a3b
  max_steps: 64  # matches Docker submission MAX_STEPS=64 (= prevents step exhaustion on heavy doc tasks)
  min_steps: 4
  temperature: 0.0

run:
  # workers=4 outer × 5 inner attempts = 20 concurrent vLLM streams.
  max_workers: 4
  task_timeout_seconds: 900
